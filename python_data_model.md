# The Python data model

---

## What does it mean to be pythonic?

Idiomatic. Idiom = A form of expression natural to a language. 

## What is the python data model

A framework. Standardises how we interface with the language. When the 
interpreter sees special syntax (e.g. double underscore) 